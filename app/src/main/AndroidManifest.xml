<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gamaliev.list">

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".list.ListActivity"
            android:label="@string/activity_list_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".list.ItemDetailsActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:label="@string/activity_list_details_name"/>

        <activity
            android:name=".colorpicker.ColorPickerActivity"
            android:theme="@style/AppTheme.NoActionBar.Fullscreen"
            android:label="@string/activity_color_picker_name"/>

    </application>

</manifest>